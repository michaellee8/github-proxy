apiVersion: v1
kind: Service
metadata:
  name: {{ include "pgh-broker.fullname" . }}
  labels:
    {{- include "pgh-broker.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pgh-broker.selectorLabels" . | nindent 4 }}
